V test ClickOnce deployment.
V test on Windows 10 by publishing: as that is what we will be using at formnext
V use config file to store IP address (so it's easier to adjust while at formnext)
V use config file to store file recording location (so it's easier to adjust while at formnext)
V don't accept new values for recording once recording is in progress as column header cannot be added
V don't allow recording if not connected
V record sequence number
V add an MTConnect logo
- show Header data
V make graph sliding window
- use config file to store sample interval (so it's easier to adjust while at formnext)
- use config file to store sliding window size
- add alarms

